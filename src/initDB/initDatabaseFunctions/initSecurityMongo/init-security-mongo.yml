version: 1.0
provider:
  name: openfaas
  gateway: http://${MASTER_IP}:31112
functions:
  init-security-mongo:
    lang: java8
    handler: ./init-security-mongo
    image: ${DOCKER_USERNAME}/init-security-mongo:latest

